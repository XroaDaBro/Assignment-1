--These are the picture i'm gonna use for the medallions


Bronze = love.graphics.newImage('Bronze.png')
Silver = love.graphics.newImage('Silver.png')
Gold = love.graphics.newImage('Gold.png')

function Medallions:init()
    -- NOTHING
end

function Medallions:update(dt)
    if Medal = Bronze then
        sounds['Medal']:play()
    end
    if Medal = Silver then
        sounds['Medal']:play()
    end
    if Medal = Gold then
        sounds['Medal']:play()
    end
end

function Medallions:render()
   
end
